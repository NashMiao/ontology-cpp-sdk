#include "AES.h"

bool AES::auth_encry(std::string msg, std::string &enc_msg, AEAD_mode mode) {

  return true;
}
bool AES::auth_decry(std::string msg, std::string &dec_msg, AEAD_mode mode) {
    
  return true;
}